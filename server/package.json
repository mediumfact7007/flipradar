{
  "name": "flipradar-api",
  "version": "0.9.0",
  "private": true,
  "description": "FlipRadar server-side marketplace adapters",
  "engines": {"node": ">=20"},
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js"
  }
}
