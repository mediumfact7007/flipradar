name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

concurrency:
  group: flipradar-android-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Apply compatible V0.14 patches
        shell: bash
        run: |
          if ! grep -q "import 'source_status.dart';" lib/v13_app.dart; then python3 tools/patch_v140_live_source_status.py; fi
          if ! grep -q 'e.live && roles.contains(e.role)' lib/v13_app.dart; then python3 tools/patch_v142_nonlive_market_guard.py; fi
          if ! grep -q 'List<double> v13CleanMarketValues' lib/v13_app.dart; then python3 tools/patch_v143_market_quality.py; fi
          if ! grep -q 'class V13MarketConfidence' lib/v13_app.dart; then python3 tools/patch_v144_confidence_model.py; fi
          if ! grep -q "ValueKey('v0144-confidence-card')" lib/v13_app.dart; then python3 tools/patch_v144_confidence_ui.py; fi
          if ! grep -q "ValueKey('v145-open-flips')" lib/v13_app.dart; then python3 tools/patch_v145_streamline.py; fi
          if ! grep -q 'maxLines: 3' lib/v13_app.dart; then python3 tools/patch_v145_multiline_search.py; fi
          if ! grep -q "ValueKey('v146-listing-resolved')" lib/v13_app.dart; then python3 tools/patch_v146_kleinanzeigen_share_price.py; fi
          if ! grep -q "ValueKey('v147-remember-deal')" lib/v13_app.dart; then python3 tools/patch_v147_deal_memory.py; fi
          python3 tools/patch_v147_deal_memory_fix.py
          if ! grep -q "ValueKey('v151-buyback-condition')" lib/v13_app.dart; then python3 tools/patch_buyback_deal_ui.py; fi
          if ! grep -q "ValueKey('v156-buyback-empty')" lib/v13_app.dart; then python3 tools/patch_buyback_empty_state.py; fi

      - name: Verify V0.14.8 boundaries
        shell: bash
        run: |
          grep -q '^version: 0.14.8+31$' pubspec.yaml
          grep -q 'in_app_purchase: ^3.3.0' pubspec.yaml
          grep -q 'google_mobile_ads: ^9.1.0' pubspec.yaml
          grep -q 'e.live && roles.contains(e.role)' lib/v13_app.dart
          grep -q 'status.isSandbox(source.id)' lib/v13_app.dart
          grep -q 'class V13MarketConfidence' lib/v13_app.dart
          grep -q 'Platform.isAndroid || Platform.isIOS' lib/v13_app.dart
          grep -q 'if (shareSub != null) return;' lib/v13_app.dart
          grep -q "ValueKey('v145-open-flips')" lib/v13_app.dart
          grep -q "ValueKey('v145-clear-search')" lib/v13_app.dart
          grep -q "ValueKey('v145-retry-sources')" lib/v13_app.dart
          grep -q 'void _retryFailed()' lib/v13_app.dart
          grep -q 'widget.onSearch(raw)' lib/v13_app.dart
          grep -q 'maxLines: 3' lib/v13_app.dart
          grep -q 'class SharedListingMeta' lib/source_registry.dart
          grep -q 'static Uri? sharedListingUrl' lib/source_registry.dart
          grep -q 'resolveSharedListing' lib/source_registry.dart
          grep -q "ValueKey('v146-listing-resolved')" lib/v13_app.dart
          grep -q "ValueKey('v147-remember-deal')" lib/v13_app.dart
          grep -q "status == 'Saved'" lib/v13_app.dart
          grep -q 'maxBuyAtCheck' lib/v13_app.dart
          grep -q 'confidenceScore' lib/v13_app.dart
          grep -q 'String v147AgeLabel' lib/v13_app.dart
          grep -q 'v148PrioritizeSaved' lib/v13_app.dart
          grep -q "ValueKey('v148-menu-" lib/v13_app.dart
          grep -q "ValueKey('v151-buyback-condition')" lib/v13_app.dart
          grep -q "ValueKey('v156-buyback-empty')" lib/v13_app.dart
          grep -q "require('./listing_resolver')" server/index.js
          grep -q "url.pathname === '/v1/listing/resolve'" server/index.js
          grep -q '^EBAY_ENV=sandbox$' server/.env.example
          ! grep -q 'unawaited(monetization.prepareAds())' lib/v13_app.dart

      - name: Verify eBay credential isolation
        shell: bash
        run: |
          ! grep -R -E 'EBAY_(SANDBOX_)?CLIENT_(ID|SECRET)' lib pubspec.yaml
          ! grep -R -E 'api(\.sandbox)?\.ebay\.com' lib

      - name: Check backend and listing resolver
        run: |
          node --check server/index.js
          node --check server/market_quality.js
          node --check server/listing_resolver.js
          node server/market_quality_test.js
          node server/listing_resolver_test.js

      - name: Smoke test sandbox and resolver boundary
        shell: bash
        run: |
          PORT=8099 node server/index.js >/tmp/flipradar-server.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID 2>/dev/null || true' EXIT
          for i in {1..20}; do
            curl -fsS http://127.0.0.1:8099/health >/tmp/health.json && break
            sleep 0.25
          done
          grep -q '"ok":true' /tmp/health.json
          grep -q '"ebay_environment":"sandbox"' /tmp/health.json
          curl -fsS http://127.0.0.1:8099/v1/status >/tmp/status.json
          grep -q '"data_kind":"sandbox_mock_data"' /tmp/status.json
          code=$(curl -sS -o /tmp/resolve.json -w '%{http_code}' 'http://127.0.0.1:8099/v1/listing/resolve?url=https%3A%2F%2Fexample.com%2Fs-anzeige%2Ffake%2F123')
          test "$code" = '400'
          grep -q 'unsupported_listing_url' /tmp/resolve.json
          kill $SERVER_PID
          wait $SERVER_PID 2>/dev/null || true

          EBAY_ENV=typo PORT=8100 node server/index.js >/tmp/flipradar-invalid-env.log 2>&1 &
          INVALID_PID=$!
          for i in {1..20}; do
            curl -fsS http://127.0.0.1:8100/health >/tmp/invalid-health.json && break
            sleep 0.25
          done
          grep -q '"ebay_environment":"sandbox"' /tmp/invalid-health.json
          kill $INVALID_PID
          wait $INVALID_PID 2>/dev/null || true

          EBAY_ENV=production PORT=8101 node server/index.js >/tmp/flipradar-production-env.log 2>&1 &
          PROD_PID=$!
          for i in {1..20}; do
            curl -fsS http://127.0.0.1:8101/health >/tmp/production-health.json && break
            sleep 0.25
          done
          grep -q '"ebay_environment":"production"' /tmp/production-health.json
          curl -fsS http://127.0.0.1:8101/v1/status >/tmp/production-status.json
          grep -q '"data_kind":"active_marketplace_listings"' /tmp/production-status.json
          grep -q '"configured":false' /tmp/production-status.json
          kill $PROD_PID
          wait $PROD_PID 2>/dev/null || true

      - name: Verify eBay sandbox secret wiring
        shell: bash
        env:
          EBAY_CLIENT_ID: ${{ secrets.EBAY_SANDBOX_CLIENT_ID }}
          EBAY_CLIENT_SECRET: ${{ secrets.EBAY_SANDBOX_CLIENT_SECRET }}
        run: |
          if [ -z "$EBAY_CLIENT_ID" ] || [ -z "$EBAY_CLIENT_SECRET" ]; then
            echo 'eBay sandbox repository secrets are not both configured; skipping wiring check.'
            exit 0
          fi
          EBAY_ENV=sandbox PORT=8102 node server/index.js >/tmp/flipradar-secret-wiring.log 2>&1 &
          SECRET_PID=$!
          trap 'kill $SECRET_PID 2>/dev/null || true' EXIT
          for i in {1..20}; do
            curl -fsS http://127.0.0.1:8102/v1/status >/tmp/secret-status.json && break
            sleep 0.25
          done
          grep -q '"configured":true' /tmp/secret-status.json
          grep -q '"environment":"sandbox"' /tmp/secret-status.json
          grep -q '"data_kind":"sandbox_mock_data"' /tmp/secret-status.json
          kill $SECRET_PID
          wait $SECRET_PID 2>/dev/null || true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate Android project
        shell: bash
        run: |
          cp -r lib /tmp/flipradar_lib
          cp pubspec.yaml /tmp/flipradar_pubspec.yaml
          rm -rf android
          flutter create . --platforms=android --org com.flipradar --project-name flipradar --no-pub
          rm -rf lib
          cp -r /tmp/flipradar_lib lib
          cp /tmp/flipradar_pubspec.yaml pubspec.yaml
          python3 tools/prepare_android_ci.py
          grep -q 'applicationId = "com.flipradar.app"' android/app/build.gradle.kts
          grep -q 'com.google.android.gms.ads.APPLICATION_ID' android/app/src/main/AndroidManifest.xml

      - name: Get packages
        run: flutter pub get

      - name: Static analysis
        run: flutter analyze --no-fatal-infos

      - name: Run all tests
        run: flutter test

      - name: Run critical regressions explicitly
        run: |
          flutter test test/v131_startup_test.dart
          flutter test test/v142_nonlive_market_guard_test.dart
          flutter test test/v143_market_quality_test.dart
          flutter test test/v144_market_confidence_test.dart
          flutter test test/v145_streamline_test.dart
          flutter test test/v146_kleinanzeigen_share_price_test.dart
          flutter test test/v147_deal_memory_test.dart
          flutter test test/v148_recheck_watchlist_test.dart
          flutter test test/v156_live_market_transparency_test.dart

      - name: Build release APK
        run: flutter build apk --release

      - name: Verify APK
        run: |
          test -s build/app/outputs/flutter-apk/app-release.apk
          ls -lh build/app/outputs/flutter-apk/app-release.apk
          sha256sum build/app/outputs/flutter-apk/app-release.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FlipRadar-V0.14.8-RECHECK-WATCHLIST-APK
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
          retention-days: 30

      - name: Persist validated V0.14.8 source
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib/v13_app.dart pubspec.yaml
          if ! git diff --cached --quiet; then
            git commit -m 'Finalize FlipRadar V0.14.8 recheck watchlist [skip ci]'
            git push origin main
          fi
